# This is Project-specific!!! For each project create a file .eslintrc and type what's below. 
# Also have to run: <<npm install --save -dev eslint-config-rallycoding>>

{
  "extends": "rallycoding"
}